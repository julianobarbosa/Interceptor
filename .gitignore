node_modules/
.build/
# Orphaned temp files (~98MB each) from interrupted `bun build --compile` runs
*.bun-build
.claude/worktrees/
dist/
extension/dist/
extension/dist-safari/
daemon/interceptor-daemon
daemon/*.exe
# Live images moved aside by build.sh's prepare_windows_outputs (Windows locks a
# running .exe against write/delete but allows rename). Reaped on a later build.
daemon/*.exe.old-*
daemon/.generated/
*.log
/tmp/
.DS_Store
interceptor-screenshot-*.jpg
interceptor-screenshot-*.png
interceptor-screenshot-*.webp
interceptor-macos-screenshot-*.jpg
interceptor-macos-screenshot-*.png
interceptor-macos-screenshot-*.webp
interceptor-ios-screenshot-*.jpg
interceptor-ios-screenshot-*.png
interceptor-ios-screenshot-*.webp
interceptor-audio-input-*.caf
prd/
scripts/auto-improve/
scripts/refresh-apple-docs.ts
scripts/flatten-apple-docs.ts
apple-developer-docs/
interceptor-bridge/.build/
safari/build/
safari/InterceptorSafari/InterceptorSafari\ Extension/Resources/
keys/
/.vscode/
/Auto\ Run\ Docs/
/Plans/
/MEMORY/
bench-head-to-head/results/
bench-head-to-head/results-archive-*/

# iOS runner dial-in identity (private key) generated at runtime — never commit.
selfIdentity.plist
# Release / notarization artifacts (never commit credentials or built installers)
notarization.env
.notarization.env
*.pkg
*.dmg
notary-log-*.json
release-notes-*.md
staging/
/release/
.interceptor/
.interceptor-research/
.maestro/
# Extension Fabric: operator-placed extensions live under
# ~/.interceptor/extensions (covered by .interceptor/ above). Also ignore a
# repo-ROOT extension drop + the operator trust config, so capability-specific
# extension payloads are never accidentally committed to the capability-blind core.
# Root-anchored so docs/extensions/ (the authoring guide) stays tracked.
/extensions/
extension-trust.json
# Belt-and-suspenders: the operator reference extension `native-managed-copy`
# (re-signs / audits a local managed COPY of owned, third-party, or Apple apps)
# must NEVER be committed to this repo. It lives only under ~/.interceptor/extensions
# (outside this repo). Re-signing or modifying Apple / third-party software may have
# licensing implications — e.g. the Apple Developer Agreement / macOS SLA
# (https://developer.apple.com/support/terms/#apple-developer-agreement) — that are
# the operator's responsibility to assess. Keeping this capability out of the repo and
# the shipped product is deliberate. Ignore it by
# name anywhere, in case a copy is ever dropped in-repo.
native-managed-copy/
**/native-managed-copy/
reports/
use-cases/deep-research-demo/
# Local browser-privacy research artifacts (ad-hoc page-injection snippets +
# their verification doc) — not part of the shipped product or the iOS surface.
/tools/
/docs/privacy-block-set-verification.md
# Apple container runtime data dir (symlinked from ~/Library/Application Support/com.apple.container/)
# Holds Linux container images + per-container VM disk state; never tracked.
.container-data/

# graphify knowledge-graph output (local analysis artifacts, never shipped)
**/graphify-out/

# ── BMAD Method ──────────────────────────────────────────────────────────
# The vendored framework and its regenerated per-IDE skill trees (~5500 files)
# are reinstalled by `bunx bmad-method install`, so only the intent an install
# replays stays tracked: recorded answers, custom modules, agent memory.
.agent/
.agents/
.opencode/
.gemini/
.pi/
.claude/skills/bmad-*/
.claude/skills/wds-*/
.github/agents/bmad-*.agent.md
_bmad/*
!_bmad/config.toml
!_bmad/custom/
!_bmad/_memory/
_bmad/**/config.user.toml
_bmad/config.toml.bak
_bmad/render/
.bmad-loop/runs/
.bmad-loop/cache/
.bmad-loop/policy.toml
